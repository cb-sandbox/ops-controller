apiVersion: "1"
version: "1"
id: "cb-sandbox-ops-controller"
description: "CloudBees CI configuration bundle for the cb-sandbox ops-controller Controller"
jcasc:
  - "jenkins.yaml"
plugins:
  - "plugins.yaml"
